package com.tes.services.environmentalofficer;

import org.springframework.stereotype.Service;

@Service
public class GetEnvironmentalPerformance
{

	/*
	 * public int getDataa( String type, String start_date, String end_date) throws ParseException{
	 * int count_days = 0;
	 * String json_data[];
	 * int total_no_of_days = 0;
	 * start_date="2016-05-31";
	 * end_date="2017-12-31";
	 * SimpleDateFormat sdf1 = new SimpleDateFormat("yyyy-mm-dd");
	 * java.util.Date date = sdf1.parse(start_date);
	 * java.sql.Date sqlStartDate = new java.sql.Date(date.getTime());
	 * java.util.Date date2 = sdf1.parse(end_date);
	 * java.sql.Date sqlendDate= new java.sql.Date(date2.getTime());
	 * long diff=sqlStartDate.getDate()-sqlendDate.getDate();
	 * //date="2017-12-30";
	 * if(type=="water")
	 * {
	 * count_days=regularSourceDataRepository.countDistinctrsDate(sqlStartDate, sqlendDate);
	 * }
	 * if($type == "water")
	 * $cquery = "SELECT COUNT(DISTINCT rs_date) FROM `regular_source_data` WHERE rs_date BETWEEN '$start_date' AND '$date'";
	 * else
	 * $cquery = "SELECT COUNT(DISTINCT input_date) FROM `regular_data` WHERE product_id = '$type' AND
	 * input_date BETWEEN '$start_date' AND '$date'";
	 * return count_days;
	 * }
	 */

}
